# 使用官方 OpenJDK 作为基础镜像
FROM openjdk:17-jdk-slim

# 设置工作目录
WORKDIR /app

# 复制 jar 包到容器
COPY mcp-server.jar /app/mcp-server.jar

# 暴露端口（根据服务端实际端口）
EXPOSE 9060

# 启动命令
CMD ["java", "-Duser.timezone=Asia/Shanghai","-jar", "mcp-server.jar"]
